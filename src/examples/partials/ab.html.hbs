{{!
    `a` is a template, so handlebars partial syntax works.
    `b` is static content, so the `get` helper must be used.
    Note that `get` may be used for templates as well.
}}
{{~> a.html.hbs}}{{get [/].b}}
