{{!
    `[/].a` is a template, so handlebars partial syntax works as a shortcut.
    `[/].b` is static content, so the `get` helper MUST be used. Note that
    `get` MAY be used for templates as well.
}}
{{~> ([/].a)}}{{get [/].b}}
